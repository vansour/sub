services:
  web:
    image: ghcr.io/vansour/sub:latest
    container_name: sub
    hostname: localhost
    build:
      context: .
      dockerfile: Dockerfile.alpine
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./config:/app/config
      - ./data:/app/data
